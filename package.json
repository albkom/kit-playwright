{
  "name": "kit-playwright",
  "version": "1.0.0",
  "description": "End-to-end tests for lib.js using Playwright",
  "scripts": {
    "test": "playwright test",
    "test-ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "dev": "npx serve . --listen 3001 --no-clipboard"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0"
  }
}
